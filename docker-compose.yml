version: '3.8'

services:
  app:
    build:
      context: .
    image: laravel-rss-feed-aggregator
    container_name: laravel_rss_feed_aggregator
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www
    working_dir: /var/www
    command: php artisan serve --host=0.0.0.0 --port=8000
